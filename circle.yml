machine:
  services:
    - docker

dependencies:
  override:
    - docker pull condaforge/linux-anvil

test:
  override:
    - bash:
        parallel: true
        files:
            - ci_support/run_circle_no_envars.sh
            - ci_support/run_circle_with_envars.sh
            - ci_support/run_docker_build.sh
